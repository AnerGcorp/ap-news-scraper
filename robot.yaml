tasks:
  - name: Install Dependencies
    pip:
      requirements: requirements.txt

  - name: Run News Scraper
    command: python main.py ${SEARCH_PHRASE} ${MONTHS}
    env:
      PYTHONPATH: ${PYTHONPATH}